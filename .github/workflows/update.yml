name: Update all puppet-lint plugins

on:
  workflow_dispatch: {}
  #push:
  #  branches:
  #    - master

env:
  GIT_AUTHOR_NAME: Release Automation
  GIT_AUTHOR_EMAIL: voxpupuli@users.noreply.github.com
jobs:
  unit:
    runs-on: ubuntu-latest
    name: Run msync against all modules
    steps:
      - uses: actions/checkout@v4
      - name: Setup ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4
          bundler-cache: true
      - name: Run msync --noop
        run: bundle exec msync update --git-base=https://github.com/
